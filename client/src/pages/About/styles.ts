import styled from "styled-components";

export const About = styled.main`
	min-height: calc(100vh - 64px);
	width: 100%;

	padding: 1rem;

	position: relative;

	h2 {
		width: 100%;
		text-align: center;
	}
`;
